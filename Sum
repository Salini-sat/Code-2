#include<stdio.h>

int sum(int *a,int *b){
    if(*a>*b){
        return *a;
    }
    else if(*b>*a){
        return *b;
    }
    else{
        return 1;
    }
}
int main(){
    int a,b;
    scanf("%d %d",&a,&b);
    int c= sum(&a,&b);
    if(c==1){
        printf("%d is greater",c);
    }
    else{
        printf("%d is greater",c );
    }
}
